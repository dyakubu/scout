[project]
name = "media"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.2",
    "transformers>=4.40",
    "huggingface-hub>=0.23",
    "pillow>=10.0",
]
