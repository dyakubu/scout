# Go build artifacts
/scout
*.exe
*.exe~
*.dll
*.so
*.dylib
/bin/
*.db*
.DS_Store

# Native deps (onnxruntime, tokenizers) fetched locally, not committed.
/third_party/

# Packaged release archives (scripts/package-release.sh output).
/dist/

# Python (media worker) build/runtime artifacts.
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# CLIP weights downloaded into media.model_dir - covers both the current
# default location and /models/media, an old default path that's no
# longer used but may still exist on disk from before that default changed.
/media/model/
/models/media/
media_embedding/